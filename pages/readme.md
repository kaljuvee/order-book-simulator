# Preparing Environment

```
$ python3.9 -m venv myenv (Linux) OR
$ python -m venv venv (win)
$ source myenv/bin/activate
$ source venv/Scripts/activate
$ pip install -r requirements.txt
$ python create_news_item.py -s biotech
```